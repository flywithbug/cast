{*para*}

import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.experimental.Accessors;

import com.ebike.gencode.base.{*para*};


/**
{*para*}
 */
@Data
@Accessors(chain = true)
@EqualsAndHashCode(callSuper = true)
public class {*para*} extends {*para*}<{*para*}> {

{*para*}

    public {*para*}() {
        super("{*para*}");
    }

    @Override
    public Class<{*para*}> getDataClazz() {
        return {*para*}.class;
    }

}